# GM Information Graph Runs

Future information propagation graph snapshots can be stored here:

```text
gm-graph-runs/<sceneId>/<runId>/round_<n>/information_graph.json
```

This is only a filesystem convention for experiment outputs.
The persona dealer does not read or write information graphs.
