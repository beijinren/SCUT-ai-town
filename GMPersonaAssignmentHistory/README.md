# GM Persona Assignment History

Runtime persona assignment history can be stored here by external scripts or future tooling:

```text
GMPersonaAssignmentHistory/<sceneId>/assignment_history.json
```

This folder is intentionally outside `convex/GM/setup/` so generated experiment records do not mix with source code.

The v1 dealer core is pure TypeScript and does not write files by itself.
